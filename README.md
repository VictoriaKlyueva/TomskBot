# Бот, который невероятно любит Томск

### How to
```bash
docker-compose up --build
```

Модуль validator будет вынесен в отдельный модуль внутри докер контейнера
Сейчас импортируется в классе ```yandex_gpt_bot```
